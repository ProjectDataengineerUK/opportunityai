name: CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  REGION: us-central1
  AR_HOST: us-central1-docker.pkg.dev
  AR_REPO: opportunityai

jobs:
  # ── Job 1: Testes — roda em todo PR e push ────────────────────────
  test:
    name: Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: backend/requirements-test.txt

      - name: Install test dependencies
        working-directory: backend
        run: pip install -r requirements-test.txt

      - name: Run tests
        working-directory: backend
        run: pytest tests/ -v --tb=short --cov=agents --cov-report=term-missing

  # ── Job 2: Infra — Terraform gerencia recursos GCP ───────────────
  # Só roda na main. gcloud run deploy (Job 3) atualiza as imagens.
  infra:
    name: Terraform
    runs-on: ubuntu-latest
    needs: test
    if: ${{ success() && github.ref == 'refs/heads/main' && github.event_name != 'pull_request' }}
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Authenticate to GCP
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_SERVICE_ACCOUNT }}

      - name: Set up gcloud
        uses: google-github-actions/setup-gcloud@v2

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "~> 1.6"

      - name: Terraform Init
        working-directory: infra/terraform
        run: |
          terraform init \
            -backend-config="bucket=${{ secrets.GCP_PROJECT_ID }}-tf-state-opportunityai" \
            -backend-config="prefix=terraform/state"

      - name: Remove orphaned secret_version state entries
        working-directory: infra/terraform
        run: |
          terraform state rm google_secret_manager_secret_version.freelancer_token_v1 || true
          terraform state rm google_secret_manager_secret_version.freelancer_client_id_v1 || true

      - name: Terraform Apply
        working-directory: infra/terraform
        run: |
          terraform apply -auto-approve \
            -var="project_id=${{ secrets.GCP_PROJECT_ID }}" \
            -var="github_repo=${{ github.repository }}"

  # ── Job 3: Deploy — build de imagens + atualiza Cloud Run ────────
  # Usa gcloud run deploy (não Terraform) para não expor segredos ao CI.
  deploy:
    name: Build & Deploy
    runs-on: ubuntu-latest
    needs: test
    if: ${{ success() && github.ref == 'refs/heads/main' && github.event_name != 'pull_request' }}
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Authenticate to GCP
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_SERVICE_ACCOUNT }}

      - name: Set up gcloud
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker ${{ env.AR_HOST }} --quiet

      - name: Build & push backend
        id: backend
        run: |
          cp config.yaml backend/config.yaml
          IMAGE=${{ env.AR_HOST }}/${{ secrets.GCP_PROJECT_ID }}/${{ env.AR_REPO }}/backend
          docker build -t ${IMAGE}:${{ github.sha }} -t ${IMAGE}:latest ./backend
          docker push ${IMAGE}:${{ github.sha }}
          docker push ${IMAGE}:latest
          echo "image=${IMAGE}:${{ github.sha }}" >> $GITHUB_OUTPUT

      - name: Build & push frontend
        id: frontend
        run: |
          IMAGE=${{ env.AR_HOST }}/${{ secrets.GCP_PROJECT_ID }}/${{ env.AR_REPO }}/frontend
          docker build -t ${IMAGE}:${{ github.sha }} -t ${IMAGE}:latest ./frontend
          docker push ${IMAGE}:${{ github.sha }}
          docker push ${IMAGE}:latest
          echo "image=${IMAGE}:${{ github.sha }}" >> $GITHUB_OUTPUT

      - name: Deploy backend to Cloud Run
        run: |
          gcloud run deploy opportunityai-backend \
            --image=${{ steps.backend.outputs.image }} \
            --region=${{ env.REGION }} \
            --project=${{ secrets.GCP_PROJECT_ID }}

      - name: Deploy frontend to Cloud Run
        run: |
          gcloud run deploy opportunityai-frontend \
            --image=${{ steps.frontend.outputs.image }} \
            --region=${{ env.REGION }} \
            --project=${{ secrets.GCP_PROJECT_ID }}

      - name: Deploy summary
        run: |
          echo "### Deploy concluído :rocket:" >> $GITHUB_STEP_SUMMARY
          echo "- **Backend:** \`${{ steps.backend.outputs.image }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Frontend:** \`${{ steps.frontend.outputs.image }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit:** \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
